# ProdTrack — План реализации

> Источник истины по требованиям — `specs/` (v1.3). Код строго соответствует спекам; любое отклонение оформляется как «предложение изменения спека» и применяется только после согласования.
>
> Детализация задач — `tasks.md`. Архитектура и стек — `tech_stack.md`.

## Порядок фаз (зависимости)

Фазы выполняются строго последовательно; внутри фазы задачи могут идти параллельно, если не связаны зависимостями. Зависимости задач всегда направлены «вверх» (к более ранним фазам), никогда назад.

| Фаза | Название | Модули | Ключевые зависимости |
|------|----------|--------|----------------------|
| 0 | Инфраструктура | — (монорепо, CI, БД, сид) | — |
| 1 | НСИ и доступ | M01, M02 | Фаза 0 |
| 2 | Планирование и производство | M03 → M04 → M05 → M06 | Фаза 1 |
| 3 | Перемещение и приёмка | M07 → M08 | Фазы 1, 2 |
| 4 | Сквозные | M09, M10, M13 | Фазы 1–3 |
| 5 | Данные для 1С | M12 | Фазы 2–4 |
| 6 | Сводный дашборд | M11 | Фазы 2–5 |

---

## Приёмка фаз и релизный процесс (ПРОЦ-01…ПРОЦ-04)

По завершении каждой фазы и при выпуске каждого релиза применяются процессные правила, зафиксированные в `process.md`:

- **ПРОЦ-01 — Самопроверка фазы.** Перед переходом к следующей фазе dsh проводит полную самопроверку и выносит вердикт «готова / не готова».
- **ПРОЦ-02 — Версионирование.** SemVer, git-теги `vX.Y.Z`, `CHANGELOG.md` и версия в UI-футере из env `VERSION`.
- **ПРОЦ-03 — Деплой на VPS.** Каждый релиз разворачивается на self-hosted VPS по артефактам `docker-compose.yml`, `.env.example`, `scripts/deploy.sh`, `docs/deploy_vps.md`.
- **ПРОЦ-04 — Ручное тестирование.** После деплоя пользователь выполняет пошаговый план из BR/UC фазы и предоставляет скриншоты-подтверждения.

Релизная рутина (тег, CHANGELOG, деплой, ручное тестирование) входит в состав работ фаз 2–6 и описана в `tasks.md`.

---

## Фаза 0. Инфраструктура

**Цель.** Поднять монорепо, CI, базу данных и сид-данные; заложить фундамент (UI-токены, авторизация-скелет), на котором строятся все модули.

**Входные критерии.**
- Спеки v1.3 зафиксированы (`specs/`).
- `tech_stack.md` согласован.

**Модули.** — (обслуживает все модули).

**Состав работ.**
- Монорепо: `apps/web`, `packages/db`, `packages/ui` (pnpm workspace, TypeScript).
- CI: lint + typecheck + тесты + сборка на PR.
- БД: PostgreSQL + Prisma, стартовая миграция, пустая БД разворачивается из миграций.
- Сид-данные (Prisma seed): 12 РЦ (Р-02), 2 склада (Р-19), справочник причин «ввода за Оператора» (Р-13), справочник причин брака, 2 смены (Р-05).
- Дизайн-токены FactoryAll в `packages/ui` (цвета, типографика, радиусы, кнопки, правила янтарного акцента).
- Скелет авторизации (сессии, middleware) — детализация в Фазе 1.

**Выходные критерии.**
- Монорепо собирается, CI зелёный.
- Пустая БД разворачивается `prisma migrate deploy`; seed выполняется идемпотентно.
- UI-токены доступны из `packages/ui`.
- Написаны стартовые unit/интеграционные тесты инфраструктуры.

**Артефакты.** Структура монорепо + конфиги CI + `packages/db` (schema.prisma, seed) + `packages/ui` (токены, базовые компоненты) + тесты.

---

## Фаза 1. НСИ и доступ (M01, M02)

**Цель.** Реализовать справочники и управление доступом — фундамент для всех бизнес-модулей.

**Входные критерии.** Фаза 0 завершена (монорепо, БД, сид, токены).

**Модули.** M01 (НСИ), M02 (пользователи/роли/доступ).

**Состав работ.**
- M01: справочники РЦ, сотрудники, номенклатура, причины брака, причины ввода за Оператора, смены, склады. Валидация `код`/`табельный_номер` (Р-20), защита складов (Р-19), `active` + запрет удаления (Р-21), деактивация с предупреждением (Р-22).
- M02: аутентификация, сессии 12 ч (Р-07), окно доступа ОПР 14 ч (Р-07), роли и матрица доступа, множественные роли (Р-23), политика паролей и первый вход (Р-15).

**Выходные критерии.** Все справочники M01 и весь доступ M02 работают; сид-данные загружены; BR M01/M02 покрыты тестами.

**Артефакты.** Роуты/экраны M01/M02, Prisma-модели справочников и User/Role/Permission/Session, middleware авторизации, unit + e2e-тесты.

---

## Фаза 2. Планирование и производство (M03 → M04 → M05 → M06)

**Цель.** Замкнуть цикл «план → факт → остатки → отчёт смены».

**Входные критерии.** Фаза 1 завершена (НСИ и доступ).

**Модули.** M03 (ПЗ) → M04 (исполнение) → M05 (остатки) → M06 (отчёт).

**Состав работ.**
- M03: статусная модель ПЗ/строк, валидация Р-02, закрытие Р-04, ввод за Оператора (Р-11/Р-13), отмена Р-12, корректировка после закрытия Р-18.
- M04: подтверждение получения, ввод факта (выпуск/потребление/брак/остановки), категория Р-01, потребление Р-10, единицы Р-14, корректировка факта Р-18.
- M05: движения остатков (приход/списание/потребление/возврат), списание при отправке Р-03, возврат Р-12, онлайн-балансы.
- M06: отчёт смены, диаграммы Р-08, блок потребления Р-10, «чч:мм» Р-05.
- Релизная рутина Фазы 2: git-тег `v1.0.0`, CHANGELOG, деплой на VPS (ПРОЦ-03), ручное тестирование (ПРОЦ-04).

**Выходные критерии.** Полный цикл производства отрабатывает на сквозном сценарии; остатки корректны; отчёт смены формируется; самопроверка фазы по ПРОЦ-01 — вердикт «готова».

**Артефакты.** Роуты/экраны M03–M06, Prisma-модели (ProductionOrder, ProductionFact, StockMovement, ShiftSummary), сервисы остатков, unit + e2e-тесты.

---

## Фаза 3. Перемещение и приёмка (M07 → M08)

**Цель.** Реализовать перемещение ГП на склад и приёмку с расхождениями.

**Входные критерии.** Фазы 1–2 завершены (остатки ГП готовы).

**Модули.** M07 (перемещение) → M08 (приёмка/расхождения).

**Состав работ.**
- M07: статусная модель Перемещения, списание при SUBMITTED (Р-03), отмена Р-12 (возврат остатков).
- M08: приёмка КСГП, расхождения, согласование УСГП/НП, приёмка отменённого невозможна (Р-12).
- Релизная рутина Фазы 3: git-тег `v1.1.0`, CHANGELOG, деплой на VPS (ПРОЦ-03), ручное тестирование (ПРОЦ-04).

**Выходные критерии.** Перемещение и приёмка (в т.ч. расхождения) работают; остатки двух складов согласованы; самопроверка фазы по ПРОЦ-01 — вердикт «готова».

**Артефакты.** Роуты/экраны M07/M08, Prisma-модели (GoodsTransfer, Discrepancy), unit + e2e-тесты.

---

## Фаза 4. Сквозные (M09, M10, M13) — параллельно

**Цель.** Реализовать уведомления, хронометраж и аудит поверх всех модулей.

**Входные критерии.** Фазы 1–3 завершены (источники событий/изменений существуют).

**Модули.** M09 (уведомления), M10 (хронометраж), M13 (аудит).

**Состав работ.**
- M09: каталог событий EV-01…EV-10, in-app центр уведомлений, кликабельные deep-link, payload EV-08 (Р-13).
- M10: хронометраж статусных переходов, длительности этапов (включая CANCELLED и ввод за Оператора).
- M13: аудит-журнал (append-only), обязательный лог Р-09 (+Р-13/Р-17/Р-18), архивация `archived` Р-16, видимость.
- Релизная рутина Фазы 4: git-тег `v1.2.0`, CHANGELOG, деплой на VPS (ПРОЦ-03), ручное тестирование (ПРОЦ-04).

**Выходные критерии.** События доходят адресатам; длительности этапов фиксируются; все значимые изменения логируются, архив работает; самопроверка фазы по ПРОЦ-01 — вердикт «готова».

**Артефакты.** SSE-эндпоинт + центр уведомлений (M09), таблицы/сервисы хронометража (M10) и аудита (M13), unit + e2e-тесты.

---

## Фаза 5. Данные для 1С (M12)

**Цель.** Реализовать рабочее место С1С (подготовка данных, отметка «обработано», CSV).

**Входные критерии.** Фазы 2–4 завершены (итоги смен и Перемещения существуют).

**Модуль.** M12.

**Состав работ.**
- Единая сущность TaskForOneC (PRODUCTION | TRANSFER) (Р-24).
- Единый список задач с фильтром по типу, колонка «последнее изменение» (Р-18).
- Отметка «обработано» + отмена с причиной (Р-17), CSV-экспорт по обоим типам (Р-06).
- Релизная рутина Фазы 5: git-тег `v1.3.0`, CHANGELOG, деплой на VPS (ПРОЦ-03), ручное тестирование (ПРОЦ-04).

**Выходные критерии.** С1С видит актуальные данные по обоим типам, отмечает/отменяет обработку, выгружает CSV; самопроверка фазы по ПРОЦ-01 — вердикт «готова».

**Артефакты.** Роут/экран M12, Prisma-модель TaskForOneC, CSV-экспорт, unit + e2e-тесты.

---

## Фаза 6. Сводный дашборд (M11) — финал

**Цель.** Свести онлайн-картину жизненного цикла в единый дашборд.

**Входные критерии.** Все предыдущие фазы завершены.

**Модуль.** M11.

**Состав работ.**
- Виджеты Р-08: статусы ПЗ текущей смены по РЦ; объёмы по категориям; объём в перемещении; принято на склад ГП за период; длительности этапов.
- Правило «производится» = ПЗ текущей смены в CONFIRMED/IN_PROGRESS.
- Real-time обновление через SSE.
- Релизная рутина Фазы 6: git-тег `v2.0.0`, CHANGELOG, деплой на VPS (ПРОЦ-03), ручное тестирование (ПРОЦ-04).

**Выходные критерии.** Дашборд отражает актуальное состояние всего контура и обновляется в реальном времени; самопроверка фазы по ПРОЦ-01 — вердикт «готова».

**Артефакты.** Роут/экран M11, SSE-виджеты, unit + e2e-тесты.

---

## Definition of Done (общая для всех задач)

1. Код соответствует спеку (BR/UC).
2. Unit-тесты покрывают BR, затронутые задачей.
3. Миграция БД (если есть) применена и протестирована на пустой БД.
4. Нет деградации производительности (N+1, лишние запросы).
5. UI использует дизайн-токены из `packages/ui`.
6. PR ссылается на задачу в `tasks.md` и BR/UC в спеках; README/CHANGELOG обновлены.
