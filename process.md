# ProdTrack — Процессные правила разработки и релизов

> Авторитетный источник — утверждённые заказчиком правила ПРОЦ-01…ПРОЦ-04. Документ обязателен к применению, начиная с завершения Фазы 2. Связанные документы: `plan.md`, `tech_stack.md §10`, `tasks.md`.
> 
> **Роли:**
> - **dsh** — автор изменений/агент разработки; выполняет автоматизируемые шаги и формирует артефакты.
> - **Пользователь** — владелец продукта/заказчик; принимает решения, предоставляет доступы и выполняет шаги, которые нельзя автоматизировать без внешних учётных данных.

---

## 1. Самопроверка фазы (ПРОЦ-01)

По завершении **каждой** фазы dsh проводит самопроверку всей реализованной фазы **перед** переходом к следующей.

### 1.1 Проверяемые артефакты

1. Качество кода:
   ```bash
   pnpm build
   pnpm lint
   pnpm typecheck
   pnpm test
   ```
   Все команды завершаются с exit 0.
2. Чистая база данных:
   ```bash
   prisma migrate deploy
   pnpm db:seed
   prisma migrate deploy
   pnpm db:seed   # повторный прогон проверяет идемпотентность
   ```
   Оба прогона `pnpm db:seed` должны завершиться без ошибок и без дублирования данных.
3. Покрытие BR фазы тестами — таблица соответствия «BR фазы → тестовый сценарий/файл».
4. Ревизия TODO: критичных блокеров (TODO с пометкой `BLOCKER`, `HACK`, `FIXME`) не осталось; некритичные задокументированы.
5. Сверка кода со спеками v1.3: расхождения — списком с указанием места и причины.
6. Отчёт с вердиктом **«фаза готова»** или **«фаза не готова»**.

### 1.2 Вердикт

- **«готова»** — если все пункты пройдены и расхождения со спеками задокументированы как предложения изменений, либо отсутствуют.
- **«не готова»** — при любом нарушении: падении CI, ошибках миграции/сида, непокрытых BR, незакрытых блокерах.

Без положительного вердикта фаза не считается завершённой, и следующая фаза не запускается.

### 1.3 Артефакт

Файл `docs/phase_N_selfcheck_report.md` (`N` — номер фазы) по шаблону из раздела «Форматы отчётов».

---

## 2. Версионирование (ПРОЦ-02)

Начиная с завершения Фазы 2, проект использует **SemVer** `X.Y.Z`.

### 2.1 Семантика версий

| Компонент | Когда инкрементируется |
|-----------|------------------------|
| `Z` (patch) | Хотфиксы после релиза — критичный баг, найденный на проде. |
| `Y` (minor) | Завершение фаз 3–5 — новый функционал, обратно совместимый. |
| `X` (major) | Продуктовые вехи — завершение Фазы 6, переломные изменения. |

### 2.2 Git-теги

- Тег формируется как `vX.Y.Z`.
- Тег ставится **на main** после успешного самопроверки фазы (ПРОЦ-01).
- CI инжектирует значение тега в env `VERSION` при сборке Docker-образа.

### 2.3 Карта релизов

| Версия | Событие | Примечание |
|--------|---------|------------|
| `v0.1.0` | Завершение Фазы 0 | Ретро-тег на коммите завершения Фазы 0. |
| `v0.2.0` | Завершение Фазы 1 | Ретро-тег на коммите завершения Фазы 1. |
| `v1.0.0` | Завершение Фазы 2 | Первый деплой на продакшен/VPS. |
| `v1.1.0` | Завершение Фазы 3 | — |
| `v1.2.0` | Завершение Фазы 4 | — |
| `v1.3.0` | Завершение Фазы 5 | — |
| `v2.0.0` | Завершение Фазы 6 | Продуктовая веха. |

### 2.4 CHANGELOG

- Ведётся по формату [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/).
- Файл `CHANGELOG.md` находится в корне репозитория.
- Каждый релиз дополняет секцию `[Unreleased]` или создаёт новую секцию `[X.Y.Z] - YYYY-MM-DD`.

### 2.5 Версия в UI

- В футере приложения отображается значение env `VERSION`.
- CI передаёт в `VERSION` значение git-тега (например, `v1.0.0`).
- Это позволяет скриншотам ручного тестирования подтверждать версию развёрнутого приложения.

### 2.6 Артефакты

- Git-тег `vX.Y.Z` на main.
- Обновлённый `CHANGELOG.md`.
- Релиз-отчёт (см. раздел «Форматы отчётов»).

---

## 3. Деплой на VPS (ПРОЦ-03)

Каждый релиз деплоится на VPS, начиная с `v1.0.0`.

### 3.1 Обязательные артефакты релиза

| Файл | Назначение |
|------|------------|
| `docker-compose.yml` | Стек: web + postgres + nginx. |
| `.env.example` | Шаблон переменных окружения для production. |
| `scripts/deploy.sh` | Автоматизированный скрипт деплоя. |
| `docs/deploy_vps.md` | Пошаговая инструкция по развёртыванию. |

### 3.2 Процедура деплоя

1. Сборка Docker-образа с тегом версии (например, `prodtrack:v1.0.0`).
2. Доставка артефактов на VPS.
3. `prisma migrate deploy` на production-базе.
4. Идемпотентный `pnpm db:seed`.
5. Смоук-тест:
   - логин доступен;
   - версия в футере совпадает с git-тегом.

### 3.3 Роли

- **dsh** выполняет деплой, если имеет доступ к VPS и учётным данным.
- **Пользователь** выполняет деплой по инструкции `docs/deploy_vps.md`, если dsh не имеет доступа.
- Результат фиксируется в релиз-отчёте.

---

## 4. Ручное тестирование (ПРОЦ-04)

После каждого деплоя проводится ручное тестирование.

### 4.1 План ручного тестирования

- Готовится пошаговый план на основе BR/UC завершённой фазы.
- Каждый шаг содержит:
  - действие пользователя;
  - ожидаемый результат;
  - требование скриншота-подтверждения.

### 4.2 Выполнение

- **Пользователь** выполняет шаги и присылает скриншоты.
- dsh помогает формализовать план и фиксирует результаты.

### 4.3 Обработка дефектов

- **Критичные баги** — хотфикс: поднимается patch-версия (`Z+1`), выполняется повторный деплой и прогон затронутого шага.
- **Некритичные** — заводятся как задачи в `tasks.md` и включаются в бэклог следующей фазы/релиза.

### 4.4 Артефакт

Файл `docs/release_vX.Y.Z_test_report.md` по шаблону ниже.

---

## 5. Форматы отчётов

### 5.1 Отчёт о самопроверке фазы

Файл: `docs/phase_N_selfcheck_report.md`

```markdown
# Самопроверка Фазы N

- Дата: YYYY-MM-DD
- dsh: <автор>
- Вердикт: готова / не готова

## 1. CI и тесты
- pnpm build: exit 0 / fail
- pnpm lint: exit 0 / fail
- pnpm typecheck: exit 0 / fail
- pnpm test: exit 0 / fail

## 2. База данных
- prisma migrate deploy: OK / fail
- pnpm db:seed ×2: OK / fail

## 3. Покрытие BR
| BR | Тест | Статус |
|----|------|--------|
| BR-XX | path/to/test.ts | pass / fail |

## 4. TODO-ревью
- Блокеров: 0 / N (список)

## 5. Расхождения со спеками v1.3
- <если есть, список с обоснованием>

## Вердикт и обоснование
<краткое заключение>
```

### 5.2 Релиз-отчёт

Файл: `docs/release_vX.Y.Z_report.md`

```markdown
# Релиз-отчёт vX.Y.Z

- Дата: YYYY-MM-DD
- Git-тег: vX.Y.Z
- Коммит: <hash>
- Деплой: выполнен dsh / выполнен пользователем / не выполнен
- Смоук: OK / fail

## Что вошло в релиз
- <список задач/фич>

## Артефакты
- docker-compose.yml: OK
- .env.example: OK
- scripts/deploy.sh: OK
- docs/deploy_vps.md: OK
- CHANGELOG.md: обновлён

## Проблемы и действия
- <если есть>
```

### 5.3 Отчёт о ручном тестировании

Файл: `docs/release_vX.Y.Z_test_report.md`

```markdown
# Ручное тестирование vX.Y.Z

- Дата: YYYY-MM-DD
- Версия в футере: vX.Y.Z
- Тестировщик: <пользователь>

## Результаты

| № | Шаг | Ожидаемый результат | Статус | Скриншот |
|---|-----|---------------------|--------|----------|
| 1 | ... | ... | pass / fail | <ссылка/имя файла> |

## Найденные дефекты
| Шаг | Критичность | Описание | Действие |
|-----|-------------|----------|----------|
| ... | critical / minor | ... | хотфикс / задача в tasks.md |
```

---

## 6. Роли и ответственность

| Процесс | dsh | Пользователь |
|---------|-----|--------------|
| ПРОЦ-01 «Самопроверка фазы» | Выполняет проверки, формирует отчёт и вердикт. | Принимает вердикт; решает, запускать ли следующую фазу. |
| ПРОЦ-02 «Версионирование» | Ставит git-тег, обновляет CHANGELOG, настраивает CI/ env VERSION. | Утверждает номер версии и содержание CHANGELOG. |
| ПРОЦ-03 «Деплой на VPS» | Деплоит, если есть доступ; иначе готовит артефакты и инструкцию. | Деплоит по инструкции при отсутствии доступа dsh; предоставляет учётные данные/VPS. |
| ПРОЦ-04 «Ручное тестирование» | Готовит план, фиксирует результаты, оформляет баги в задачи. | Выполняет шаги и предоставляет скриншоты-подтверждения. |
