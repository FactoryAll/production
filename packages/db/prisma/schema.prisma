// Placeholder Prisma schema for T-003
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Placeholder {
  id String @id @default(uuid())
}
